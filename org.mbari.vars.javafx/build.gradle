plugins {
    id "org.openjfx.javafxplugin"
}

dependencies {
    compile project(':org.mbari.vars.services')
}

javafx {
    version = javafxVersion
    modules = [
            'javafx.controls',
            'javafx.fxml'
    ]
}
